"use server";

/*
export async function sendFileToParser(formData: FormData) {
    const response = await fetch(`${process.env.PARSER_URL}/api/File`, {
        method: "POST",
        body: formData,
    });
    if (!response.ok) {
        throw new Error("Ошибка при отправке файла");
    }
    const json = await response.json();
    return JSON.stringify(json);
}
 */
